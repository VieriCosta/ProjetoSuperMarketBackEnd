{
    "configurations": [
        {
            "type": "node-terminal",
            "name": "Executar Script: start",
            "request": "launch",
            "command": "npm start",
            "cwd": "${workspaceFolder}"
        }
    ]
}